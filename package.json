{
  "name": "fixwale-monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "FixWale – Home services marketplace (mobile, API, admin)",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm -ws run build",
    "dev": "npm -ws run dev",
    "lint": "npm -ws run lint",
    "test": "npm -ws run test"
  },
  "engines": {
    "node": ">=18.17.0",
    "npm": ">=9.0.0"
  }
}
